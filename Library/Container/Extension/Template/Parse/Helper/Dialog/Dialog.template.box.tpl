<div id="ContainerExtensionTemplateParseCreateDialog_{$id}" class="ContainerExtensionTemplateParseCreateDialog_container card-container card-container--shadow"
     data-automaticClose="{$automaticClose}"
     data-showautomaticcloseprocess="{$automaticClose}">
    <div class="header flex-container card-container-header {$headerClass}">
        <div class="header_content">{$header}</div>
        <div class="header_close dialog_close">{$closeIcon}</div>
    </div>
    <div class="body {$bodyClass}">
        {$body}
    </div>
    <div class="footer card-container-footer {$footerClass}">
        {$footer}
    </div>
</div>
